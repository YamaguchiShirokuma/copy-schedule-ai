import type { ExtractedCalendarEvent } from '../ai/schema';
const scope = 'https://www.googleapis.com/auth/calendar.events';
export function googleAuthUrl() { const { GOOGLE_CLIENT_ID, GOOGLE_REDIRECT_URI } = process.env; if (!GOOGLE_CLIENT_ID || !GOOGLE_REDIRECT_URI) throw new Error('Google OAuth設定が未設定です。GOOGLE_CLIENT_ID と GOOGLE_REDIRECT_URI を設定してください。'); const u = new URL('https://accounts.google.com/o/oauth2/v2/auth'); u.searchParams.set('client_id', GOOGLE_CLIENT_ID); u.searchParams.set('redirect_uri', GOOGLE_REDIRECT_URI); u.searchParams.set('response_type', 'code'); u.searchParams.set('scope', scope); u.searchParams.set('access_type', 'offline'); u.searchParams.set('prompt', 'consent'); return u.toString(); }
export async function exchangeCode(code: string) { const { GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI } = process.env; if (!GOOGLE_CLIENT_ID || !GOOGLE_CLIENT_SECRET || !GOOGLE_REDIRECT_URI) throw new Error('Google OAuth設定が未設定です。Client ID/Secret/Redirect URIを設定してください。'); const r = await fetch('https://oauth2.googleapis.com/token', { method:'POST', headers:{'Content-Type':'application/x-www-form-urlencoded'}, body: new URLSearchParams({ code, client_id: GOOGLE_CLIENT_ID, client_secret: GOOGLE_CLIENT_SECRET, redirect_uri: GOOGLE_REDIRECT_URI, grant_type:'authorization_code' }) }); if (!r.ok) throw new Error('Google OAuthトークン交換に失敗しました。'); return r.json(); }
export async function createGoogleEvent(accessToken: string, event: ExtractedCalendarEvent) { const body: Record<string, unknown> = { summary: event.title, location: event.location || undefined, description: `${event.description}\n\n--- sourceText ---\n${event.sourceText}` };
  if (event.isAllDay) { const d = (event.startDateTime || '').slice(0,10); body.start = { date: d }; body.end = { date: d }; } else { body.start = { dateTime: event.startDateTime, timeZone: 'Asia/Tokyo' }; body.end = { dateTime: event.endDateTime || event.startDateTime, timeZone: 'Asia/Tokyo' }; }
  const r = await fetch('https://www.googleapis.com/calendar/v3/calendars/primary/events', { method:'POST', headers:{ Authorization:`Bearer ${accessToken}`, 'Content-Type':'application/json' }, body: JSON.stringify(body) }); if (!r.ok) throw new Error(await r.text()); return r.json(); }
